#Create Database

* login to mysql
> myql -u admin -p

* create database SIS;  // you can name whatever you like

* use database SIS;

*  CREATE TABLE users (ID MEDIUMINT NOT NULL AUTO_INCREMENT PRIMARY KEY, username VARCHAR(60),email VARCHAR(100), password VARCHAR(60))
